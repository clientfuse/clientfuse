/**
 * Token for injecting the payment provider implementation
 *
 * @example
 * constructor(
 *   @Inject(PAYMENT_PROVIDER_TOKEN) private paymentProvider: IPaymentProvider
 * ) {}
 */
export const PAYMENT_PROVIDER_TOKEN = 'PAYMENT_PROVIDER';
